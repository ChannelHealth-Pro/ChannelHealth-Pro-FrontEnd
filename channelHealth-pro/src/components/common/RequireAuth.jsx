import { Navigate } from "react-router-dom";
// import { useGlobal } from "../../GlobalContext";

function RequireAuth() {
  // const { user } = useGlobal();

  // if (!user) {
  //   return <Navigate to="/channelinghome" />;
  // }

  return;
}

export default RequireAuth;
